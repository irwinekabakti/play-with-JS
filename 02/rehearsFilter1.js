let driver = [
  {
    name: "Max",
    team: "RB",
    poin: 310,
  },
  {
    name: "Leclerc",
    team: "Ferrari",
    poin: 201,
  },
  {
    name: "Sir Lewis",
    team: "Meredes",
    poin: 158,
  },
  {
    name: "Alonso",
    team: "Alpine",
    poin: 59,
  },
];

/*
const filter1 = (driver) => {
  let newArr = [];
  for (let i = 0; i < driver.length; i++) {
    if (driver[i].poin > 100) {
      newArr.push(driver[i]);
    }
  }
  return newArr;
};

console.log(filter1(driver));
*/

/*
const filter2 = (driver) => {
  let newArr1 = [];
  for (let i = 0; i < driver.length; i++) {
    if (driver[i].poin > 100 && driver[i].poin > 200) {
      newArr1.push(driver[i]);
    }
  }
  return newArr1;
};

console.log(filter2(driver));
*/

// const filter3 = (driver) => {
//   for (let i = 0; i < driver.length; i++) {
//     for (let j = 0; j < driver.length - 1; j++) {
//       console.log(driver[i][j]);
//     }
//   }
//   return driver;
// };

// console.log(filter3(driver));
